@model IEnumerable<MidCapERP.Dto.AccessoriesTypes.AccessoriesTypesResponseDto>
@{
    ViewData["Title"] = "AccessoriesType";
}

<div class="c-card--wrapper">
    <div class="c-card--header">
        <h4 class="c-card--header-title">AccessoriesType</h4>
        <div class="c-card--header-action">
            <a class="c-card--header-action-btn btn btn-outline-primary" id="lnkAccessoriesTypesFilter"><i class='bx bx-plus me-1'></i>Filters</a>
            <a asp-controller="AccessoriesTypes" asp-action="Create"
               class="btn btn-primary" data-ajax="true" data-ajax-mode="replace"
               data-ajax-complete="AccessoriesTypesModel.onComplete" data-ajax-update="#divUpdateAccessoriesTypes">Create New</a>
        </div>
    </div>
    <div class="c-card--body">
        <div class="card mb-4 p-3" id="FilterCard">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="form-label">AccessoriesTypes Name</label>
                        <input type="text" class="form-control" placeholder="AccessoriesType Name" />
                    </div>
                </div>
            </div>
        </div>
        <partial name="_AccessoriesTypesTable" model="@Model" />
        <div id="divUpdateAccessoriesTypes"></div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
        await Html.RenderPartialAsync("_JqueryDatatableScriptsPartial");

        <script src="~/js/AccessoriesTypes.js" asp-append-version="true"></script>
    }
}