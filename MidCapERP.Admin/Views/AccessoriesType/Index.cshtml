@model IEnumerable<MidCapERP.Dto.AccessoriesType.AccessoriesTypeResponseDto>
@{
    ViewData["Title"] = "Accessories Types";
}

<div class="c-card--wrapper">
    <div class="c-card--header">
        <h4 class="c-card--header-title">Accessories Types</h4>
        <div class="c-card--header-action">
            <a class="c-card--header-action-btn btn btn-outline-primary" id="lnkAccessoriesTypeFilter"><i class='bx bx-plus me-1'></i>Filters</a>
            <a asp-controller="AccessoriesType" asp-action="Create"
               class="btn btn-primary" data-ajax="true" data-ajax-mode="replace"
               data-ajax-complete="AccessoriesTypeModel.onComplete" data-ajax-update="#divUpdateAccessoriesType">Create New</a>
        </div>
    </div>
    <div class="c-card--body">
        <div class="card mb-4 p-3 filter-form--wrapper" id="FilterCard">
            <div class="row">
                <div class="col-md-4">
                    <div class="form-group">
                        <label class="form-label">AccessoriesType Name</label>
                        <input type="text" class="form-control" placeholder="AccessoriesType Name" />
                    </div>
                </div>
            </div>
        </div>
        <partial name="_AccessoriesTypeTable" model="@Model" />
        <div id="divUpdateAccessoriesType"></div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
        await Html.RenderPartialAsync("_JqueryDatatableScriptsPartial");

        <script asp-src-include="~/js/AccessoriesType.js" ></script>
    }
}